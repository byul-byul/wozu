Вот полный, строгий план доведения модуля `auth` в WOZU до **идеального уровня**, с разбивкой по срочности:

---

## 🔴 **НУЖНО СДЕЛАТЬ СЕЙЧАС** *(блокеры, важная надёжность)*

### 🔐 Безопасность и контроль

* [ ] **Ограничить доступ для `user.is_active = False`**
  → Через зависимость `require_active_user()`
* [ ] **Rate-limit на регистрацию и подтверждение**
  → Ограничить частоту создания токенов, задержка между повторными письмами

### 🐛 Обработка ошибок и логирование

* [ ] **Логирование ошибок (`try/except`) во всех критичных функциях**
  → `register_user()`, `verify_email()`, `login_user()`
  → Логгер через `logging` или простые `print()` с контекстом
* [ ] **Грамотные ответы клиенту (422, 401, 403, 500)**
  → Вместо `raise Exception()` — `HTTPException`

### ✅ Тестирование

* [ ] **Юнит-тесты на `register_user()`**
* [ ] **Юнит-тесты на `verify_email()`**
* [ ] **Покрытие: валидные кейсы, ошибки, повтор, просроченные токены**

---

## 🟡 **МОЖНО ОТЛОЖИТЬ ДО ПРОДАКШЕНА** *(важно, но не критично сейчас)*

### 🔁 Refresh Token

* [ ] **Добавить refresh-токены и обновление access-токена**

### 📆 Очистка и TTL

* [ ] **Автоудаление/очистка просроченных `EmailToken`**
  → Через cron, Celery, или `DELETE ... WHERE expires_at < now() AND is_used = FALSE`

### 🔒 2FA (позже)

* [ ] Поддержка подтверждения по **телефону** (SMS или WhatsApp)

### 📬 Повторная отправка письма

* [ ] Добавить ручку: `POST /auth/resend-email-token`

---

## 🟢 **НЕОБЯЗАТЕЛЬНО, НО ХОРОШО ИМЕТЬ** *(для красоты, удобства, улучшений)*

### 📊 Отладка и наблюдаемость

* [ ] Уровни логов (`DEBUG`, `INFO`, `ERROR`)
* [ ] Отправка ошибок в Sentry или лог-файл

### 🧪 Покрытие

* [ ] Тесты на ручки (`/auth/register`, `/verify-email`, `/login`) через `httpx.AsyncClient`
* [ ] Тесты на rate-limit / edge-cases

### 🧰 CLI/инструменты

* [ ] Команда в manage.py или FastAPI CLI для ручной активации юзера по email
* [ ] Генерация dev-токена для тестов без email

### 🧼 Красота

* [ ] Приятные сообщения об ошибках (`"Invalid or expired token."`)
* [ ] HTML-шаблон письма адаптирован под бренд WOZU
* [ ] README.md в папке `auth/` с описанием всех ручек и flow

---

## 📌 Вывод:

| Категория               | Кол-во задач |
| ----------------------- | ------------ |
| **Нужно сейчас**        | 7            |
| **До продакшена**       | 4            |
| **Опционально/красота** | 7            |

Хочешь — могу начать с конкретной задачи и показать её реализацию (например, логирование в `verify_email`).
